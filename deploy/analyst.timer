# Запускать Аналитика раз в 2 дня (см. опорный документ). Включить:
#   sudo systemctl daemon-reload && sudo systemctl enable --now analyst.timer
[Unit]
Description=Run Fabrika Analyst every 2 days

[Timer]
OnCalendar=*-*-01/2 09:00:00
Persistent=true

[Install]
WantedBy=timers.target
